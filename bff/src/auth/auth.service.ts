import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  // Este serviço pode ser expandido para incluir lógica de login ou registro
  // que interage com o backend, ou apenas para orquestrar a validação JWT.
  // Por enquanto, ele serve como um placeholder.
}
